# Copy to mcp/.env and fill in locally. Never commit real values.
# Full HTTPS endpoint of the owner's Coolify instance, ending in /mcp.
COOLIFY_MCP_URL=
# Dedicated Coolify token with read permission only; no deploy or root.
COOLIFY_API_TOKEN=
